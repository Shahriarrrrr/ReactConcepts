/**
 * 1. Use form tag and onSubmit handler with event(e)
 * Form e access e.target.[name of the input field].value
 * 
 * 
 * 2. controlled element: use individual field state for each 
 * input field
 * 
 * 
 * 3. Controlled element: one obj. is in a state holding all the input 
 * fields
 * 
 * 4.uncontrolled element:  useRef to create a reference to the element and access 
 * value by using like: nameRef.current.value
 */